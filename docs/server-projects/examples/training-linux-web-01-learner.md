# 演習案件01: 本人による実行と結果確認

[演習手順](training-linux-web-01.md) / [本人が保存した実行結果](training-linux-web-01-learner-result.json)

## 実施範囲と根拠

2026-09-09、本人がUbuntuのWSL端末で、AIが提示したコマンドを実行した。
Python 3.12.3の確認、スクリプトの所在確認、実行、終了コードの確認、
日時付きJSONへの保存、保存結果の読み取りまでを、対話中の画面と本人の回答で確認した。
本文書はその対話をCodexが整理したもの。独力での構築能力や第三者の検収を認定するものではない。

| 項目 | 記録 |
| --- | --- |
| 種別 | training / AIの手順・説明による支援あり |
| 実行環境 | Ubuntu WSL2 / Python 3.12.3 |
| 保存元のファイル名 | result-20260909-153857.json |
| 実行開始（UTC） | 2026-09-09T06:38:57.856225+00:00 |
| 実行終了（UTC） | 2026-09-09T06:38:58.490769+00:00 |
| 実行結果 | 総合PASS、10/10項目PASS |
| 終了コード | 本人が直後のecho $?で0と回答。JSONには含まれない |
| 対象コード | JSONのscript_sha256を実行スクリプトと照合 |

添付JSONは本人のローカル保存ファイルから内容を変更せずコピーした。
`actor` はスクリプトが固定出力する `AI-assisted execution; not learner assessment` のままである。
このフィールドだけでは操作者を識別できない。本人による操作の根拠は対話中の画面と回答であり、
スクリプトによる本人認証ではない。端末画像は利用者名・端末名・ローカルパスを含むため添付しない。
既存の[AI実行記録](training-linux-web-01-result.json)は別の実行として保持する。

## 本人が読み取った結果

| 確認 | 期待と実際 | 結果 |
| --- | --- | --- |
| T02 変更前のHTTP本文 | 200、version 1 | 一致 |
| T05 変更後のHTTP本文 | 200、version 2 | 一致 |
| T06 復元後のHTTP本文 | 200、version 1 | 一致 |
| T07 復元ファイルのハッシュ | 変更前と復元後が一致 | True |
| T08 サーバーの処理停止 | 処理スレッドが停止 | PASS |
| T09 HTTP応答停止 | 停止後のリクエストが到達しない | PASS |
| T10 一時領域の清掃 | 一時フォルダーが存在しない | PASS |

これらは保存された実行時点の記録を読み取った結果であり、後日の現在状態を再測定した結果ではない。
HTTP 200だけでは本文の正しさは分からないこと、バックアップ保存だけでは復元確認にならないこと、
ハッシュ不一致なら復元成功と判断できないことを、説明と問い返しを通して確認した。

## 本人の作業報告（原文）

> 実施したこと：スクリプト実行・結果保存・結果の読み取り
>
> 確認できたこと：本文の変更と復元、ハッシュの一致、停止・後片付けまで確認できた
>
> まだ実施していないこと：専用VMの構築

## 完了した範囲と次の工程

今回完了したのは、演習01の「手順付きスクリプト実行・結果保存・結果の読み取り」。
自動スクリプト内のWeb起動・変更・復元を、一つずつ本人が手動操作したわけではない。
本人の独力での構築・復旧、専用VM構築、Ansible、Docker、systemd、firewall、
再起動、長期監視、第三者による検収、PJ00〜PJ07の工程承認はNOT RUN。
この記録では案件台帳や承認を更新せず、独立した確認役の記録を作らない。

次の演習は専用Ubuntu VMの構築。最初の作業はHyper-Vマネージャーを開けるかの確認で、
今回の対話ではその結果はまだ確認していない。既存VMの選定・設定変更・削除も未実施。
